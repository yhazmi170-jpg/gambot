PENDING SMALL UPDATES (not yet announced)
-------------------------------------------
Queue small updates here as they ship. When this hits ~10 items (or the user says so),
flush: move them into update_msg.txt as ONE combined log + bump package.json version,
then push (the boot announcement posts it once).

Format: one line per change, terse, no AI fluff.
(flushed into v1.7.5)
- Intel sync endpoints: GET/POST /intel?key= records+serves 24/7 message activity to the selfbot (discord-selfy) for alt-link + last-moves tracking. Internal, no player-facing change.
- Selfbot health watcher: gambot DMs the owner when the selfbot comes online, restarts (uptime reset), or goes offline — DM-only, never posts to update channels. New `Aovo selfbot` (status) and `Aovo selfbot restart` (Render redeploy) owner commands. Internal/owner-facing.
- Selfbot health alerts now require 3 failed checks before offline and 2 healthy checks before recovery; overlapping checks are skipped to prevent timeout/recovery DM spam.
- Selfbot health alerts debounced: restart DM only on version change + 6h cooldown on all alert types (was DMsing the owner every ~30 min on false uptime resets).
- bal: crash on `v bal @user` for users with no account (null ensureUser) -> now graceful 'user not found'
- slb: fetch real server member list on demand (+ GuildMembers intent) so the server leaderboard shows the correct members/amounts for THIS guild only.
- status endpoint now reports guild ids/member counts (diagnostic for silent-server issue).
- Owner manual give/remove (Aovo add/give/remove, Aovo gems add/remove) now posts an audit log entry to the guild's log channel (set via 'Aovo log #channel') — records actor, target, amount, action. Internal/owner-facing.
- Fix silent unregistered-user responses: TOS prompt now wrapped in try/catch so failures can't silently swallow commands (debug the new-server zero-response).
- Debug: log every parsed command (guild/chan/user/cmd) + capture async handleMessage rejections; diagnosing members getting no reply in snowed #cmds.
- HARD GUARD: bot now refuses to boot anywhere except Render (prevents local double-instance that wiped definity's data).

(flushed into v1.8.0)
- Pet weapons shipped (weaponcrate + weapon list/equip/upgrade + battle effects + hunt/battle drops + shop item). Full detail in CHANGELOG / update_msg.

(flushed into v1.8.1)
- Coinflip clarity fix: reply shows picked vs landed, correct win amount.
- TOS 'I Agree' ack-race fix.
- Short aliases: wc, wep, z, bd, hst, lck, rn, sb, rwd, gp.

(queued after v1.8.1)
- v profile stat card expanded: level/xp, pets+team, bank, eggs & seals, battle wins, daily streak, lucky status, insurance tier, free bet, credit score, loan/clan/plot/jail (when active), member since.
- FIX: credit score on profiles read the wrong DB column (showed a raw timestamp). ensureUser now maps columns by name -> jail state + rob cooldown reads also fixed.
- New fun command: v 8ball / v 8b <question> - 45 silly magic answers, compact embed quoting your question (long questions auto-truncate).
- 8ball: reply-only format with a per-answer self-reaction (😭/☠️ by vibe, plus sparing 💔/🙏/🤞/⁉️ — never random) on Gambot's own reply; answer pool expanded 45 → 120 diabolical/casual lines (roasts, dry dismissals, loaded no's), 'gang' → 'gng', no mystical filler.
- v give to the owner: the owner alone gets a DM approval card (Keep / Decline / Mute 30m). Keep closes the card, Decline refunds the sender, Mute blocks that sender from gifting the owner for 30 min ("u cant give this user money"). Money moves on confirm; owner decides after. Owner+internal only.

(queued after v2.0.3)
- FIX: bot was online but unresponsive to ALL commands (ReferenceError: getLastMeaningfulAt/getLastSummonAt were never defined + a const reassignment) -> every command died silently after cooldown_ok. Both fixed in the command handler.
- FIX: giveaways that END never finalized (100M split stuck with no winners/no payout/no announce) -> draw + payout reservation + exact-once inbox delivery + single announcement now all idempotent across restarts.
- Gaming parity restored to intended 2.0.3: dice/mines/roulette lucky-gambling fully removed for real (a rollback had re-added it), coinflip was stuck at an 0.8 win-chance -> now true 50/50 even money, gamehelp text scrubbed of lucky.
- New debug endpoints: /logs (last 100 console lines), /giveaways (read-only giveaway state), /guilddebug fixed (was double-writing headers and exposing nothing).
- FIX: v lb must include the money in the bank — shows + ranks wallet+bank (money in bank counted), consistent for everyone. Regression test: money=100/bank=900 -> lb shows 1000.
- Owner gets a DM when anyone buys a shop item: "<@user> bought **Item** for `price` money!"
