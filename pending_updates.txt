PENDING SMALL UPDATES (not yet announced)
-------------------------------------------
Queue small updates here as they ship. When this hits ~10 items (or the user says so),
flush: move them into update_msg.txt as ONE combined log + bump package.json version,
then push (the boot announcement posts it once).

Format: one line per change, terse, no AI fluff.
- Clan creation now costs 5M coins (was 500k).
- New weekly Gambling Leaderboard: `Aovo lb #channel` picks a channel, bot auto-posts the top 10 gamblers every week and pays rewards (1st 1M → 10th 40k). Owner stays hidden. View anytime with `v glb`.
- Clans are now PLAYER-based, not server-based: `v clan create` makes a clan owned by you, `v clan join @me|name` joins anyone's clan, `v clan delete` disbands (50% treasury back). One clan per player, 5M to create.
- NEW `v pokertournament <buy-in>` — 5-card poker showdown table (9 seats), best hand wins the whole pot (90%), join via button.
- NEW Travelling merchant — rolls in every ~90min, sells 1 rare pet + gems + essence, first come first served via `v merchant`.
- NEW Clan Wars — `v clanwar challenge <name> <stake>`: both clans pay a treasury stake, members click Fight, the winning clan takes both stakes. 1M–25%. No-show = refunded.
- Custom roles now force-stack directly under the anchor role (failed position moves are logged instead of silently ignored).
- FIXED: `v clanwar challenge` crashed with "an error occurred" right after creating the war (the accept/decline buttons failed to build). Challenges now post their board, and stale/unanswered challenges auto-expire after 60s.
