services:
  - type: web
    name: gambot
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node index.js
    healthCheckPath: /health
    envVars:
      - key: TOKEN
        sync: false
      - key: GITHUB_TOKEN
        sync: false
      - key: DB_PATH
        value: ./data
